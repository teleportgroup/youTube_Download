<Window x:Class="YouTubeDownloader.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available"
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Update Available"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,10"/>

        <!-- Version info -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Foreground="{DynamicResource TextSecondaryBrush}">
                <Run Text="Current version: "/>
                <Run x:Name="CurrentVersionText" FontWeight="SemiBold"/>
            </TextBlock>
            <TextBlock Foreground="{DynamicResource TextPrimaryBrush}">
                <Run Text="New version: "/>
                <Run x:Name="NewVersionText" FontWeight="SemiBold"/>
            </TextBlock>
        </StackPanel>

        <!-- Changelog -->
        <GroupBox Grid.Row="2" Header="What's New" Foreground="{DynamicResource TextSecondaryBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="5">
                <TextBlock x:Name="ChangelogText"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Progress bar (hidden by default) -->
        <StackPanel Grid.Row="3" x:Name="ProgressPanel" Visibility="Collapsed" Margin="0,15,0,0">
            <TextBlock x:Name="ProgressText"
                       Text="Downloading update..."
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="0,0,0,5"/>
            <ProgressBar x:Name="DownloadProgress"
                         Height="6"
                         Background="{DynamicResource BorderBrush}"
                         Foreground="{DynamicResource AccentBrush}"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="RemindLaterButton"
                    Content="Remind Me Later"
                    Click="RemindLater_Click"
                    Padding="16,8"
                    Margin="0,0,10,0"
                    Background="{DynamicResource ButtonSecondaryBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="DownloadButton"
                    Content="Download &amp; Install"
                    Click="Download_Click"
                    Padding="16,8"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
